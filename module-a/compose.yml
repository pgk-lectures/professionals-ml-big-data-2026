services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: professionals
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - professionals_pgdata:/var/lib/postgresql/data

volumes:
  professionals_pgdata:
